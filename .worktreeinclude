# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# local sqlite db
/prisma/*.db
/prisma/*.db-shm
/prisma/*.db-wal
*.db
*.db-shm
*.db-wal

# Claude Code memory
# (.claude/skills symlinks are force-tracked in git so they survive into worktrees;
#  the copy mechanism here does not preserve symlinks, so do NOT list .claude/skills)
.claude/agents
.claude/launch.json
.claude/settings.json
.claude/settings.local.json